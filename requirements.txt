# UI
streamlit>=1.36,<2

# YAML parsing
PyYAML>=6.0

# Graph + messages
langgraph>=0.2,<1.0
langchain-core>=0.2,<1.0
langchain-openai>=0.1.7,<1.0

# Needed for structured outputs + LC compat layer
pydantic>=1.10,<3

# OpenAI client (langchain-openai depends on it, but pinning avoids resolver surprises)
openai>=1.30,<3
